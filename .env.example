# LLM providers (only needed if using API mode instead of CLI mode)
ANTHROPIC_API_KEY=replace_me
OPENAI_API_KEY=replace_me
OPENAI_MODEL=gpt-4o

# Gemini CLI runs locally; for API access via SDK:
GOOGLE_GENAI_API_KEY=replace_me
GEMINI_MODEL=gemini-2.5-pro

# GitHub CLI for PR automation
GITHUB_TOKEN=replace_me

# Repository root (absolute path) and dev branch
REPO_PATH=/home/umwai/um-agent-orchestration
DEV_BRANCH=dev

# Redis (optional, defaults to localhost:6379)
REDIS_HOST=localhost
REDIS_PORT=6379

# Slack webhook for alerts (optional)
SLACK_WEBHOOK_URL=